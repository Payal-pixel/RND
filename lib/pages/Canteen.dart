import 'package:flutter/material.dart';

class canteen extends StatelessWidget {
  const canteen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      appBar: new AppBar(
        title: new Text("Select canteen"),
      ),
    );
  }
}
